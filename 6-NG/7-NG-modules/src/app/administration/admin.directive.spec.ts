import { AdminDirective } from './admin.directive';

describe('AdminDirective', () => {
  it('should create an instance', () => {
    const directive = new AdminDirective();
    expect(directive).toBeTruthy();
  });
});
