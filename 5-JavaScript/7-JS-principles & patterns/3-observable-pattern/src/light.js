


const light = {
    on(e) {
        console.log(`LIGHT ON with door-num : ${e.num} `);
    },
    off(e) {
        console.log(`LIGHT OFF with door-num : ${e.num} `);
    }
};

export default light;